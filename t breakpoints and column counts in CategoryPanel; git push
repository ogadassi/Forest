[1mdiff --git a/frontend/src/components/board/CategoryPanel.tsx b/frontend/src/components/board/CategoryPanel.tsx[m
[1mindex 2449612..0217393 100644[m
[1m--- a/frontend/src/components/board/CategoryPanel.tsx[m
[1m+++ b/frontend/src/components/board/CategoryPanel.tsx[m
[36m@@ -14,8 +14,8 @@[m [mimport { restrictToVerticalAxis } from '@dnd-kit/modifiers';[m
 [m
 const ResponsiveGridLayout = Responsive as any;[m
 [m
[31m-const BREAKPOINTS = { lg: 1200, md: 996, sm: 768, xs: 480, xxs: 0 };[m
[31m-const COLS = { lg: 12, md: 10, sm: 6, xs: 4, xxs: 2 };[m
[32m+[m[32mconst BREAKPOINTS = { lg: 700, md: 500, sm: 350, xs: 200, xxs: 0 };[m
[32m+[m[32mconst COLS = { lg: 12, md: 8, sm: 4, xs: 2, xxs: 1 };[m
 [m
 interface GridItem {[m
     i: string; x: number; y: number;[m
[36m@@ -38,9 +38,9 @@[m [mfunction buildDefaultNoteLayouts(notes: NoteModel[], existing: Layouts): Layouts[m
         const missing = notes.filter(n => !existingIds.has(String(n.id)));[m
         const validBase = base.filter(l => notes.some(n => String(n.id) === l.i));[m
 [m
[31m-        let w = 4;[m
[31m-        if (bp === 'md') w = 5;[m
[31m-        if (bp === 'sm') w = 6;[m
[32m+[m[32m        let w = 6;[m
[32m+[m[32m        if (bp === 'md') w = 4;[m
[32m+[m[32m        if (bp === 'sm') w = 4;[m
         if (bp === 'xs' || bp === 'xxs') w = COLS[bp as keyof typeof COLS];[m
 [m
         const colsForBp = COLS[bp as keyof typeof COLS];[m
[36m@@ -49,17 +49,26 @@[m [mfunction buildDefaultNoteLayouts(notes: NoteModel[], existing: Layouts): Layouts[m
         const newItems: GridItem[] = missing.map((note, idx) => {[m
             const rawContent = typeof note.content === 'string' ? note.content : JSON.stringify(note.content || '');[m
             const txtLen = rawContent.replace(/<[^>]*>?/gm, '').length;[m
[31m-            const extraHeight = Math.floor(txtLen / 250);[m
[31m-            const calculatedH = Math.min(4 + extraHeight, 12); // Cap at h=12 so it doesn't get unmanageable[m
[32m+[m[32m            const extraHeight = Math.floor(txtLen / 150);[m
[32m+[m[32m            const calculatedH = Math.min(8 + extraHeight, 18); // default h=8 (~160px), cap at 18[m
 [m
             return {[m
                 i: String(note.id),[m
                 x: ((validBase.length + idx) % itemsPerRow) * w,[m
[31m-                y: Math.floor((validBase.length + idx) / itemsPerRow) * 5, // Push down enough to avoid overlaps[m
[31m-                w, h: calculatedH, minW: 1, minH: 3,[m
[32m+[m[32m                y: Math.floor((validBase.length + idx) / itemsPerRow) * 9,[m
[32m+[m[32m                w, h: calculatedH, minW: 1, minH: 10,[m
             };[m
         });[m
[31m-        newLayouts[bp] = [...validBase, ...newItems];[m
[32m+[m[32m        // Enforce min constraints on existing saved items so old layouts[m
[32m+[m[32m        // don't render as thin slivers when the panel size changes.[m
[32m+[m[32m        const sanitizedBase = validBase.map(item => ({[m
[32m+[m[32m            ...item,[m
[32m+[m[32m            w: Math.min(item.w, colsForBp),[m
[32m+[m[32m            h: Math.max(item.h, 8),[m
[32m+[m[32m            minW: 1,[m
[32m+[m[32m            minH: 8,[m
[32m+[m[32m        }));[m
[32m+[m[32m        newLayouts[bp] = [...sanitizedBase, ...newItems];[m
     });[m
     return newLayouts;[m
 }[m
[36m@@ -165,6 +174,7 @@[m [mexport const CategoryPanel: React.FC<CategoryPanelProps> = ({[m
     const [containerWidth, setContainerWidth] = useState(0);[m
     const [isDragging, setIsDragging] = useState(false);[m
     const [isResizing, setIsResizing] = useState(false);[m
[32m+[m[32m    const lastInteractionTimeRef = useRef(0);[m
 [m
 [m
     useEffect(() => {[m
[36m@@ -248,7 +258,10 @@[m [mexport const CategoryPanel: React.FC<CategoryPanelProps> = ({[m
     return ([m
         <div[m
             className={`category-panel-root h-full flex flex-col rounded-2xl overflow-hidden select-none relative ${isFullscreenView ? '' : 'cursor-pointer'}`}[m
[31m-            onClick={!isFullscreenView ? onHeaderClick : undefined}[m
[32m+[m[32m            onClick={!isFullscreenView ? () => {[m
[32m+[m[32m                if (Date.now() - lastInteractionTimeRef.current < 300) return;[m
[32m+[m[32m                onHeaderClick?.();[m
[32m+[m[32m            } : undefined}[m
             style={{[m
                 containerType: 'size',[m
                 background: `linear-gradient(160deg, ${color}0C 0%, rgba(26,32,27,0) 55%), #1e261f`,[m
[36m@@ -333,15 +346,14 @@[m [mexport const CategoryPanel: React.FC<CategoryPanelProps> = ({[m
                                 />[m
                             </form>[m
                         ) : ([m
[31m-                            <div className="flex items-center gap-2">[m
[31m-                                <span className="font-bold text-sm text-slate-100 group-hover:text-white transition-colors truncate">{category.name}</span>[m
[31m-                                <button[m
[32m+[m[32m                            <div className="flex items-center min-w-0">[m
[32m+[m[32m                                <span[m
[32m+[m[32m                                    className="font-bold text-sm text-slate-100 group-hover:text-white transition-colors truncate cursor-text"[m
                                     onClick={(e) => { e.stopPropagation(); setIsEditing(true); }}[m
[31m-                                    className="opacity-0 group-hover:opacity-100 p-1 hover:bg-white/10 rounded transition-all text-slate-400 hover:text-white"[m
[31m-                                    title="Rename category"[m
[32m+[m[32m                                    title="Click to rename"[m
                                 >[m
[31m-                                    <span className="material-icons-round text-[14px]">edit</span>[m
[31m-                                </button>[m
[32m+[m[32m                                    {category.name}[m
[32m+[m[32m                                </span>[m
                             </div>[m
                         )}[m
                     </div>[m
[36m@@ -604,10 +616,10 @@[m [mexport const CategoryPanel: React.FC<CategoryPanelProps> = ({[m
                         width={containerWidth}[m
                         useCSSTransforms={true}[m
                         measureBeforeMount={false}[m
[31m-                        onDragStart={() => setIsDragging(true)}[m
[31m-                        onDragStop={() => setTimeout(() => setIsDragging(false), 50)}[m
[31m-                        onResizeStart={() => setIsResizing(true)}[m
[31m-                        onResizeStop={() => setTimeout(() => setIsResizing(false), 50)}[m
[32m+[m[32m                        onDragStart={() => { lastInteractionTimeRef.current = Date.now(); setIsDragging(true); }}[m
[32m+[m[32m                        onDragStop={() => { lastInteractionTimeRef.current = Date.now(); setTimeout(() => setIsDragging(false), 50); }}[m
[32m+[m[32m                        onResizeStart={() => { lastInteractionTimeRef.current = Date.now(); setIsResizing(true); }}[m
[32m+[m[32m                        onResizeStop={() => { lastInteractionTimeRef.current = Date.now(); setTimeout(() => setIsResizing(false), 50); }}[m
                     >[m
                         {notes.map(note => ([m
                             <div key={String(note.id)} className="relative group">[m
